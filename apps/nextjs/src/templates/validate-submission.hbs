<html>
  <head>
    <style>
      body { font-family: Arial, sans-serif; margin: 0; padding: 20px;
      background-color: #f4f4f4; } .container { max-width: 600px; margin: auto;
      background: white; padding: 20px; border-radius: 8px; box-shadow: 0 0 10px
      rgba(0, 0, 0, 0.1); } .header { font-size: 24px; margin-bottom: 20px;
      color: #333; } .content { font-size: 16px; line-height: 1.6;
      margin-bottom: 20px; color: #555; } .action-button { display:
      inline-block; background-color: #007bff; color: white !important; padding:
      12px 24px; text-decoration: none; border-radius: 5px; font-weight: bold;
      margin: 20px 0; } .details { background-color: #f8f9fa; padding: 15px;
      border-radius: 5px; margin: 15px 0; } .footer { font-size: 14px; color:
      #666; margin-top: 20px; }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="header">Validate Your F3 Submission</div>
      <div class="content">
        <p>Please click the button below to validate your submission for the
          following event:</p>

        <div class="details">
          <p><strong>Event:</strong> {{eventName}}</p>
          <p><strong>Region:</strong> {{regionName}}</p>
          <p><strong>Date:</strong> {{dayOfWeek}}</p>
          <p><strong>Time:</strong> {{startTime}} - {{endTime}}</p>
          <p><strong>Location:</strong> {{address}}</p>
          <p><strong>Type:</strong> {{type}}</p>
        </div>

        <a
          href="{{url}}/validate-submission?submissionId={{submissionId}}&token={{token}}"
          class="action-button"
        >
          Validate Submission
        </a>

        <p class="footer">
          If you did not request this validation, please ignore this email.
        </p>
      </div>
    </div>
  </body>
</html>